# bigint-buffer (GHSA-3gc7-fjrx-p6mg) is replaced at install time via the
# @solana/buffer-layout-utils override in package.json. The postinstall script
# (scripts/postinstall.js) copies the native BigInt shim into place so the
# override is resolvable at runtime. bigint-buffer is not installed.

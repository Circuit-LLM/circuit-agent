# @solana/buffer-layout-utils is overridden via patches/solana-buffer-layout-utils-0.2.0.tgz
# (see local_modules/buffer-layout-utils/ for the source). This replaces the
# bigint-buffer C native addon (GHSA-3gc7-fjrx-p6mg) with native Node.js BigInt,
# removing bigint-buffer from the dependency tree entirely.
